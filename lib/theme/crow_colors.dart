import 'package:flutter/material.dart';

/// Exact palette ported from `res/values/colors.xml`.
/// Neon accents — same roles as ClawMikia's palette.
class CrowColors {
  CrowColors._();

  static const Color pureBlack = Color(0xFF000000);
  static const Color bg = Color(0xFF0A0A0F);
  static const Color surface = Color(0xFF12121A);
  static const Color surfaceElevated = Color(0xFF1C1C28);
  static const Color onBg = Color(0xFFE8E8F0);
  static const Color onMuted = Color(0xFF7A7A9A);
  static const Color divider = Color(0xFF1E1E2E);

  static const Color accentYellow = Color(0xFFF5D547); // primary
  static const Color accentCyan = Color(0xFF00E5FF); // info
  static const Color accentBlue = Color(0xFF4A90E8); // secondary
  static const Color accentGreen = Color(0xFF39FF14); // pitch
  static const Color accentOrange = Color(0xFFFF8C00); // trim/speed
  static const Color accentRed = Color(0xFFFF2D55); // play
  static const Color accentPurple = Color(0xFFBF5FFF); // enhancement
  static const Color accentPink = Color(0xFFF21505); // skips

  static const Color timelineYellow = Color(0xFFE6C200);
  static const Color timelineBlue = Color(0xFF3B7DD6);
}
